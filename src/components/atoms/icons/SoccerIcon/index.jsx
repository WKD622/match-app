import React from 'react';
import SportsSoccerIconMaterialUi from '@material-ui/icons/SportsSoccer';

const SoccerIcon = (props) => (
    <SportsSoccerIconMaterialUi {...props} />
);

export default SoccerIcon;
